# 外贸跟单工作台 — 服务端版本部署指南（傻瓜式）

部署完成后效果：
- 得到一个固定的公网链接（如 `https://waimao-workbench.onrender.com`）
- 任何设备（电脑/手机/平板）打开这个链接，输入密码 `siyueyue`，看到的是**同一份数据**
- 在手机上修改订单，电脑上立刻同步，不用手动导出导入
- 顶部有「工作台 / 看板」两个 Tab：**在工作台改了订单，切到看板立刻看到最新数据**（看板从同一份数据实时渲染）；Render 休眠唤醒后数据从 GitHub 私有 Gist 自动恢复，不会丢失

---

## 整体流程（3 个平台）

```
你本地写好的代码  →  GitHub（存代码）  →  Render（免费运行服务器）
```

一共要注册/登录 2 个网站：GitHub（存代码）、Render（运行服务）。都是免费、不用绑卡。

---

## 第 1 步：注册 GitHub（存代码的地方）

1. 打开 https://github.com
2. 点右上角 **Sign up**（注册）
3. 用邮箱注册一个账号（用户名随便起，比如 waimao-user）
4. 验证邮箱，登录进去

---

## 第 2 步：在 GitHub 新建仓库

1. 登录后，点右上角 **+** 号 → 选 **New repository**（新建仓库）
2. 填写：
   - **Repository name**（仓库名）：`waimao-workbench`（随便起，英文即可）
   - **Description**（描述）：可留空
   - 选 **Public**（公开）—— 注意：代码公开不代表你的订单数据公开，订单数据存在 Render 服务器上，不在 GitHub 里
   - 勾选 **Add a README file**（加一个说明文件）
3. 点 **Create repository**（创建仓库）

---

## 第 3 步：把代码上传到 GitHub

> 你本地的 `waimao-server` 文件夹里需要上传的文件只有这些（**不用上传 `node_modules`**，那是依赖，Render 会自动安装）：
> ```
> server.js
> package.json
> package-lock.json
> render.yaml
> public/login.html
> public/workbench.html
> ```

**方法 A：GitHub 网页直接上传（推荐，不用装软件）**

1. 进入刚创建的仓库页面
2. 点 **Add file** → **Upload files**
3. 把上面的 `server.js`、`package.json`、`package-lock.json`、`render.yaml` 直接拖进上传区
4. 再点 **Add file** → 下拉选第一个"Create new file"，在文件名处输入 `public/login.html`，把本地 `public/login.html` 的内容粘贴进去；同样方法创建 `public/workbench.html`
   - 小技巧：文件名里带 `/` 就会自动创建文件夹，所以写 `public/login.html` 就会生成 `public` 文件夹
5. 全部传完后，拉到最下面，点 **Commit changes**（提交）

**方法 B：用 GitHub Desktop（图形界面，适合文件多的情况）**

1. 下载安装 GitHub Desktop：https://desktop.github.com
2. 登录你的 GitHub 账号
3. File → Add Local Repository → 选你本地的 `waimao-server` 文件夹
4. 点 **Publish repository**（发布仓库），按提示推送到 GitHub
   - 记得取消勾选 "Keep this code private" 之外的选项，直接发布

---

## 第 3.5 步：生成 GitHub 访问令牌（关键！数据存这里）

> 你的订单数据会存到一个**私有 Gist**（只有你的令牌能访问）。必须生成一个 Personal Access Token 给服务器用。

1. 登录 GitHub，点右上角头像 → **Settings**
2. 左侧最底下找到 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. 点 **Generate new token** → **Generate new token (classic)**
4. **Note** 填 `waimao-workbench`；**Expiration** 选 `30 days`（或用 No expiration，但建议定期轮换）
5. 在权限里勾选 **gist**（必须勾，服务器要读写你的私有 Gist）；
   如果你还打算用工具 / AI 把代码**推送到这个仓库**，就**同时勾选 repo**（Full control of private repositories）。
   只部署到 Render、不推送代码的话，只勾 gist 就够了。
6. 拉到底点 **Generate token**
7. 生成的令牌形如 `ghp_xxxxxxxxxxxx`，**立刻复制保存**（只显示这一次！）
8. 下一步部署时把这个令牌填进 Render 的 `GITHUB_TOKEN` 环境变量

> ⚠️ 这个令牌只用于服务器读写你的私有 Gist，不要泄露给别人。它和登录密码 `siyueyue` 是两回事。

---

## 第 4 步：注册 Render（运行服务器的地方）

1. 打开 https://render.com
2. 点 **Sign Up** → 选 **Sign up with GitHub**（用 GitHub 账号登录，最省事）
3. 授权 Render 访问你的 GitHub 账号

---

## 第 5 步：一键部署（最关键的一步）

1. 登录 Render 后，点右上角 **New** → **Blueprint**（蓝图部署）
2. 连接你的 GitHub 账号，找到 `waimao-workbench` 仓库
3. Render 会自动读取仓库里的 `render.yaml` 配置文件
4. 确认信息无误后，点 **Apply**（应用）
5. 等 2~3 分钟，Render 会自动：
   - 安装依赖（npm install）
   - 启动服务器（npm start）
   - 分配一个公网链接，格式类似 `https://waimao-workbench.onrender.com`

> 如果你没用 Blueprint，而是选了 **New Web Service**，手动填：
> - **Build Command**：`npm install`
> - **Start Command**：`npm start`
> - **Instance Type**：选 **Free**（免费）
> - 在 **Advanced** → **Environment Variables** 里加：
>   - `PASSWORD` = `siyueyue`
>   - `GITHUB_TOKEN` = 第3.5步生成的令牌（**必填**，没它数据不存）
>   - `GIST_ID` = 留空（首次启动自动创建，见下方说明）

**关于 GIST_ID（重要）**：
- 部署后第一次启动，服务器会自动创建一个**私有 Gist** 存数据，并把 Gist ID 打印在 Render 的 **Logs** 里（形如 `=== NEW GIST CREATED: xxxxx ===`）
- 请把这个 ID 复制，填进 Render 环境变量 `GIST_ID` 并保存 → 服务重启后会一直用这个 Gist，**数据彻底不丢**
- 如果不填 GIST_ID：每次 Render 休眠唤醒会新建 Gist，旧数据还在旧 Gist 里但服务器读不到 → 表现为"数据又丢了"。所以**务必填一次 GIST_ID**

---

## 第 6 步：验证 + 使用

1. 打开 Render 给你的链接（如 `https://waimao-workbench.onrender.com`）
2. 会跳到登录页，输入密码 `siyueyue`
3. 进入工作台，功能跟之前一模一样
4. 用手机浏览器打开同一个链接 → 同样输入密码 → 看到的是同一份数据 ✅

---

## ⚠️ 免费版注意事项

1. **休眠机制**：免费版 15 分钟没人访问会自动"睡觉"，下次打开会慢 10~30 秒（唤醒中）。这是正常的，等一会儿就好。
2. **免费额度**：免费版每月 750 小时，一个人用完全够，不会断。
3. **数据存在 GitHub 私有 Gist**：订单数据存在你的 GitHub 私有 Gist 里（不在 Render 磁盘），Render 怎么重启、休眠唤醒都不会丢，换设备也同步。
4. **备份建议**：虽然数据在服务器上了，仍建议偶尔点「导出 JSON」存一份到本地，以防极端情况。

---

## 想改密码怎么办？

方法一（最简单）：在 Render 后台 → 你的服务 → **Environment** → 改 `PASSWORD` 的值 → 保存 → 服务会自动重启。
方法二：改本地 `server.js` 第 7 行 `var PASSWORD = process.env.PASSWORD || '新密码';`，重新上传到 GitHub，Render 会自动重新部署。

---

## 常见问题

**Q：打开链接显示 "Application Error"？**
A：等 1 分钟刷新；或到 Render 后台看 **Logs**（日志）有没有报错。最常见是文件没传全（少了 `public` 文件夹）。

**Q：打开后没看到登录页 / 一直转圈？**
A：现在登录是**工作台页面内的弹窗遮罩**（不是单独的登录页）。只要 Render 服务正常、`healthCheckPath` 是 `/`，打开链接就会先弹出密码框，输入 `siyueyue` 即可进入。如果一直转圈，是 Render 正在"唤醒"（免费版休眠后首次访问要等 10~30 秒），稍候刷新即可。也可直接访问 `/login.html` 验证服务是否存活。

**Q：GitHub 上传时 public 文件夹不见了？**
A：用方法 A 时，文件名写成 `public/login.html`（带斜杠）就会自动建文件夹；用方法 B 则直接拖整个文件夹最省事。

**Q：数据是公开的吗？**
A：不公开。GitHub 代码仓库是公开的，但订单数据存在你的**私有 Gist** 里（只有你的令牌能访问），别人看不到。网页访问仍需密码 `siyueyue`，建议部署后改掉。
